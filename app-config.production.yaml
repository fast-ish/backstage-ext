app:
  baseUrl: ${APP_BASE_URL}

organization:
  name: ${ORGANIZATION_NAME}

backend:
  baseUrl: ${BACKEND_BASE_URL}
  listen:
    port: 7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: ${CORS_ORIGIN}
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  database:
    client: pg
    connection:
      host: ${POSTGRES_HOST}
      port: ${POSTGRES_PORT}
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}

auth:
  environment: production
  providers:
    github:
      production:
        clientId: ${AUTH_GITHUB_CLIENT_ID}
        clientSecret: ${AUTH_GITHUB_CLIENT_SECRET}

techdocs:
  builder: 'local'
  generator:
    runIn: 'local'
  publisher:
    type: 'local'

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location, Domain, Group, User, Template]
  locations:
    # Local catalog entities (bundled in Docker image)
    - type: file
      target: /app/catalog/users.yaml
      rules:
        - allow: [User, Group]
    # GitHub template repositories
    - type: url
      target: https://github.com/fast-ish/python-service-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/java-service-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/react-frontend-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/rails-api-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/data-pipeline-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/mcp-server-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/autonomous-coding-agent-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/team-onboarding-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]
    - type: url
      target: https://github.com/fast-ish/go-cli-template/blob/main/catalog-info.yaml
      rules:
        - allow: [Template, Component, API, Location, System, Domain, Resource]

kubernetes:
  serviceLocatorMethod:
    type: 'multiTenant'
  clusterLocatorMethods:
    - type: 'config'
      clusters: []
